  reverseList() {
    let temp;
    let holder = this.front;
    while (holder !== null) {
      temp = holder.prev;
      holder.prev = holder.next;
      holder.next = temp;
      holder = holder.prev;
    }
    if (temp != null) {
      this.front = temp.prev;
    }
  }